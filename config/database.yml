# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)

development:
  database: idyle_development
  adapter: mysql
  host: localhost
  username: tyler
  password: magic
  port: 5432

# Warning: The database defined as 'test' will be erased and
# re-generated from your development database when you run 'rake'.
# Do not set this db to the same as development or production.
test:
  database: idyle_test
  adapter: mysql
  host: localhost
  username: tyler
  password: magic
  port: 5432

production:
  adapter: mysql
  host: localhost
  username: idylenet_idyle
  password: lebowski
  database: idylenet_idyle
